export const mutations = {
  set_statistics_summary(state, statistics) {
    state.statistics_summary = statistics
  },
  set_loading(state, loading) {
    state.loading = loading
  },
  set_locale(state, locale) {
    state.locale = locale
  }

}
